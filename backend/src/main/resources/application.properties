spring.application.name=coperti

# Profile used when none is specified explicitly
spring.profiles.default=dev

# Disable OpenAPI docs and UI by default
springdoc.swagger-ui.enabled=false
springdoc.api-docs.enabled=false